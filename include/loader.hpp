#pragma once

#include <string>

// Loader class: loads or stores transformed data
class Loader {
    public:
    // Load/output the processed data
    void load(const std::string& output);
};