#pragma once

#include <string>

// Transformer class: simulates transforming input data
class Transformer {
    public:
    // Transforms input string to another form (uppercase here)
    std::string transform(const std::string& input);
};